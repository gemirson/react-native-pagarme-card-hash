describe('requestCardHashKey', () => {
  it('should request card_hash_key', async () => {});
});

describe('generateQueryString', () => {
  it('should generate a query string', () => {});

  it('should remove spaces from card fields', () => {});
});

describe('generateCardHash', () => {
  it('should generate card_hash', () => {});
});
